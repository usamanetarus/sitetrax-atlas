FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ app/

# PORT is set by Cloud Run; default to 9000 for local testing
ENV PORT=9000

CMD exec uvicorn app.mcp_http_server:asgi_app --host 0.0.0.0 --port ${PORT}
